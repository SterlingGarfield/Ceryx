# Ceryx v0.3 W3 Pairing and Discovery Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement local-network discovery, pairing, trusted devices, token validation, reconnect behavior, and permission-aware request guards.

**Architecture:** Discovery publishes Agent metadata through mDNS. Pairing creates trusted devices only after Windows-side confirmation and code verification. Token validation and permission checks become middleware used by all control APIs from W4 onward.

**Tech Stack:** .NET 10, ASP.NET Core filters/middleware, SQLite repositories, DNS-SD/mDNS library, cryptographic token hashing, Vitest client SDK tests.

---

## Success Criteria

- Agent publishes `_ceryx-agent._tcp.local` metadata.
- Pairing request, confirm, expiry, lockout, and trusted-device persistence work.
- Authenticated requests reject missing, invalid, or deleted tokens.
- Client SDK supports pairing and reconnect state.
- iPad/Desktop connection screens can consume discovery and pairing APIs.

## Files

- Create: `agent/windows/src/Ceryx.Agent.Network/Discovery/`
- Create: `agent/windows/src/Ceryx.Agent.Security/Pairing/`
- Create: `agent/windows/src/Ceryx.Agent.Security/Devices/`
- Create: `agent/windows/src/Ceryx.Agent.Security/Tokens/`
- Create: `agent/windows/src/Ceryx.Agent.Security/Permissions/`
- Modify: `packages/client-sdk/src/AgentClient.ts`
- Create: `packages/client-sdk/src/pairing.test.ts`
- Create: `docs/api/pairing-and-auth.md`

## Task 1: Discovery Publisher

- [x] Publish `_ceryx-agent._tcp.local`.
- [x] Include `deviceName`, `agentVersion`, `platform`, `httpPort`, `supportsWebRTC`, `supportsDesktopClient`, `codexStatus`.
- [x] Stop publishing on clean shutdown.
- [x] Add tests for metadata serialization.

Run:

```powershell
dotnet test agent/windows/Ceryx.Agent.Windows.sln --filter Discovery
```

Expected: TXT metadata matches backend requirements.

## Task 2: Pairing State Machine

- [x] Implement pairing states: `idle`, `waiting_desktop_confirm`, `code_input`, `verifying`, `success`, `expired`, `rejected`, `network_error`.
- [x] Pairing code is six digits and expires after 120 seconds.
- [x] Allow only one pending pairing request at a time.
- [x] Lock pairing for 5 minutes after 5 failed attempts.
- [x] Add audit logs for request, confirmation, failure, success, expiry.

Run:

```powershell
dotnet test agent/windows/Ceryx.Agent.Windows.sln --filter Pairing
```

Expected: expiry, failure lockout, and success paths pass.

## Task 3: Trust Store and Tokens

- [x] Add repository for `paired_devices`.
- [x] Generate `deviceId` and `deviceToken` on successful pairing.
- [x] Store token hash, never plain token.
- [x] Delete trusted device and invalidate all associated sessions.
- [x] Add default permission sets for `ipad` and `desktop`.

Run:

```powershell
dotnet test agent/windows/Ceryx.Agent.Windows.sln --filter Token
dotnet test agent/windows/Ceryx.Agent.Windows.sln --filter TrustedDevices
```

Expected: deleted devices cannot authenticate.

## Task 4: API Guards

- [x] Implement authentication filter for all protected routes.
- [x] Permit unauthenticated access only to `/api/v1/health` and `/api/v1/pairing/request`.
- [x] Add permission guard with explicit permission per control endpoint.
- [x] Standardize error responses for `E_NOT_PAIRED`, `E_TOKEN_INVALID`, and `E_PERMISSION_DENIED`.

Run:

```powershell
dotnet test agent/windows/Ceryx.Agent.Windows.sln --filter Permission
```

Expected: protected APIs reject unauthenticated requests.

## Task 5: SDK Pairing Support

- [x] Add `requestPairing`, `confirmPairing`, `connect`, `listDevices`, and `deleteDevice`.
- [x] Add typed error handling for token invalidation.
- [x] Add reconnect helper with first retry at 1s, exponential backoff, max 30s.
- [x] Add SDK tests for token persistence hooks and auth headers.

Run:

```powershell
pnpm --filter @ceryx/client-sdk test
pnpm --filter @ceryx/client-sdk typecheck
```

Expected: SDK pairing and reconnect tests pass.

## Acceptance Checklist

- [x] iPad can discover Agent metadata through a test discovery adapter.
- [x] Desktop can call pairing APIs through SDK.
- [x] Unpaired clients cannot call Agent status if the route is marked protected.
- [x] Pairing code expiry and deletion behavior are tested.
- [x] W3 does not implement remote control input or capture.

## Status Backfill (2026-05-24)

- [x] Checklist backfilled against current repository facts.
- [x] Evidence links:
  - `docs/architecture/Ceryx_v0.3_阶段性进度报告_W1-W6_2026-05-22.md`
  - `docs/release/v0.3-acceptance-report.md`
  - `agent/windows/src/Ceryx.Agent.Network/Discovery/MdnsAgentDiscoveryPublisher.cs`
  - `agent/windows/src/Ceryx.Agent.Security/Pairing/PairingStateMachine.cs`
  - `agent/windows/src/Ceryx.Agent.Security/Pairing/PairingCompletionService.cs`
  - `agent/windows/src/Ceryx.Agent.Storage/Devices/SqliteTrustedDeviceStore.cs`
- [x] Desktop Tauri packaging blocker resolved:
  - Added `apps/desktop/src-tauri/Cargo.toml`, `apps/desktop/src-tauri/build.rs`, `apps/desktop/src-tauri/src/main.rs`.
  - Added `apps/desktop/src-tauri/icons/icon.ico`.
  - Added `@tauri-apps/cli` in `apps/desktop/package.json`.
  - Verified `powershell -ExecutionPolicy Bypass -File .\scripts\build-all.ps1` full chain passed.
  - Synced to GitHub: commit `882fc58` on `origin/master`.


