# Ceryx v0.3 W8 Release Acceptance Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce a reviewable v0.3 release package with Desktop + Agent installer path, iPad build path, acceptance report, known issues, and operator documentation.

**Architecture:** W8 does not add new product scope. It packages the already verified build, checks installation/runtime behavior, and turns the implementation into a handoff-ready artifact set.

**Tech Stack:** pnpm, Tauri build, Capacitor iOS build path, dotnet publish, Windows installer script, release docs, smoke test scripts.

---

## Success Criteria

- Desktop app and Windows Agent can be built from a clean checkout.
- Windows release package includes Desktop and Agent.
- iPad build path is documented and repeatable.
- Acceptance checklist maps back to frontend, backend, and technical requirements.
- Known issues and non-goals are explicit.
- Final verification commands pass or documented blockers are precise.

## Files

- Create: `scripts/build-all.ps1`
- Create: `scripts/smoke-local-desktop.ps1`
- Create: `scripts/smoke-agent-api.ps1`
- Create: `docs/release/v0.3-release-notes.md`
- Create: `docs/release/v0.3-acceptance-report.md`
- Create: `docs/release/v0.3-known-issues.md`
- Create: `docs/operator/windows-agent.md`
- Create: `docs/operator/ipad-app.md`
- Modify: `README.md`

## Task 1: Build Scripts

- [x] Create `scripts/build-all.ps1`.
- [x] Build frontend packages through `pnpm build`.
- [x] Build Desktop through Tauri build command.
- [x] Publish Agent through `dotnet publish`.
- [x] Emit build output paths at the end of the script.

Run:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\build-all.ps1
```

Expected: script exits non-zero on any failed build and prints artifact paths on success.

## Task 2: Smoke Scripts

- [x] Create Agent API smoke script that checks `/api/v1/health` and `/api/v1/agent/status`.
- [x] Create local Desktop smoke script that starts Agent, opens Desktop dev/prod build, and checks local status.
- [x] Keep smoke scripts read-only except expected logs/build artifacts.
- [x] Document required ports and expected environment variables.

Run:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\smoke-agent-api.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\smoke-local-desktop.ps1
```

Expected: smoke scripts report pass/fail with exact failing step.

## Task 3: Desktop + Agent Packaging

- [x] Package Desktop App with Tauri.
- [x] Package Agent as self-contained Windows executable.
- [ ] Ensure installer path includes Agent binary and config defaults.
- [ ] Verify first launch creates app data directories.
- [ ] Verify logs can be opened from Desktop and tray.

Run:

```powershell
pnpm --filter @ceryx/desktop build
dotnet publish agent/windows/src/Ceryx.Agent.App/Ceryx.Agent.App.csproj -c Release
```

Expected: release artifacts are created without development-only paths.

## Task 4: iPad Build Path

- [ ] Verify Capacitor config includes local network, microphone, photo library, file, and notification permission descriptions. **Blocked:** `apps/ipad` currently has web-only scaffold (no committed Capacitor iOS native config files to verify).
- [x] Run web build for iPad app.
- [x] Document iOS native build requirements.
- [x] Record any local machine limitations for Xcode/iOS packaging.

Run:

```powershell
pnpm --filter @ceryx/ipad build
```

Expected: iPad web assets build successfully; native packaging notes are complete.

## Task 5: Acceptance Report

- [x] Map backend acceptance: Agent start, health, mDNS, SQLite, logs, pairing, Desktop integration, Codex control, media, diff, performance.
- [ ] Map frontend acceptance: iPad discovery/pairing/live/diff/upload/voice/screenshot/recording/reconnect; Desktop local Agent/control/diff/logs/settings/shortcuts/context menu/status bar. **Partial:** acceptance report currently contains high-level frontend mapping, detailed feature-by-feature matrix pending.
- [x] Map security acceptance: token validation, permission checks, audit logs, no default desktop capture, one active controller.
- [x] Include commands run, dates, environment, pass/fail result, and exact blockers.

Run:

```powershell
pnpm typecheck
pnpm test
pnpm build
dotnet test agent/windows/Ceryx.Agent.Windows.sln
```

Expected: acceptance report is backed by actual command results.

## Task 6: Release Notes and Known Issues

- [x] Release notes state what v0.3 includes.
- [x] Known issues state what remains incomplete or environment-dependent.
- [x] Non-goals repeat that v0.3 does not implement cloud relay, account system, multiplayer workspace, Plan/Schedule/Channel, or default full-screen capture.
- [x] Operator docs explain how to start/stop Agent, connect Desktop, connect iPad, find logs, export diagnostics, and revoke trusted devices.

Run:

```powershell
pnpm test
dotnet test agent/windows/Ceryx.Agent.Windows.sln
```

Expected: release docs match actual implemented behavior.

## Acceptance Checklist

- [x] Build all script succeeds.
- [ ] Smoke scripts succeed on local machine. **Blocked:** valid smoke token and full local execution evidence pending.
- [ ] Desktop + Agent package can start after installation/build extraction. **Pending:** installer/extraction runtime verification evidence not yet recorded.
- [x] iPad app web build succeeds and native build path is documented.
- [x] Release notes, acceptance report, known issues, and operator docs are complete.
- [x] No new feature scope is added during W8.

## Status Backfill (2026-05-24)

- [x] Checklist backfilled against current repository facts.
- [x] Desktop Tauri blocker resolved:
  - Added `apps/desktop/src-tauri/Cargo.toml`, `apps/desktop/src-tauri/build.rs`, `apps/desktop/src-tauri/src/main.rs`.
  - Added `apps/desktop/src-tauri/icons/icon.ico`.
  - Added `@tauri-apps/cli` in `apps/desktop/package.json`.
  - Verified `powershell -ExecutionPolicy Bypass -File .\scripts\build-all.ps1` full chain passed.
  - Synced to GitHub: commit `882fc58` on `origin/master`.
- [x] Evidence links:
  - `docs/release/v0.3-acceptance-report.md`
  - `docs/release/v0.3-release-notes.md`
  - `docs/release/v0.3-known-issues.md`
  - `docs/operator/windows-agent.md`
  - `docs/operator/ipad-app.md`
  - `scripts/build-all.ps1`
  - `scripts/smoke-agent-api.ps1`
  - `scripts/smoke-local-desktop.ps1`
