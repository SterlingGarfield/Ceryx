# Ceryx v0.3 Acceptance Report

## Date and Environment

- Date: 2026-05-28
- Repo: `D:\PrivateProjects\Ceryx_Project_Files\Ceryx\Ceryx_v0.2\CeryxProject`
- OS: Windows (PowerShell)

## Commands and Results

### Workspace verification

- `npm run typecheck` -> PASS
- `npm run test` -> PASS
- `npm run build` -> PASS

### Agent verification

- `dotnet test agent/windows/Ceryx.Agent.Windows.sln -c Release --nologo` -> PASS
- Result summary: failed 0, passed 89, skipped 0

### Agent verification backfill (2026-05-28)

- Repo-local private SDK installed under:
  - `.cache/dotnet-sdk-10.0.300/`
- Restore path recovered with:
  - `.\.cache\dotnet-sdk-10.0.300\dotnet.exe restore .\agent\windows\tests\Ceryx.Agent.Tests\Ceryx.Agent.Tests.csproj --nologo` -> PASS
- Transport suite verified with:
  - `.\.cache\dotnet-sdk-10.0.300\dotnet.exe test .\agent\windows\tests\Ceryx.Agent.Tests\Ceryx.Agent.Tests.csproj -c Release --nologo --no-restore` -> PASS
- Result summary:
  - failed 0, passed 92, skipped 0
- Root cause of prior blocker:
  - machine-level `C:\Program Files\dotnet\sdk\10.0.300` workload import/resolver path was broken
  - stale `agent/windows/**/obj/*.tmp` files also caused restore `Access denied`
  - repository verification is now unblocked through the repo-local SDK path

### W8 packaging workflow

- `powershell -ExecutionPolicy Bypass -File .\scripts\build-all.ps1` -> PASS
  - Produced staged package: `.workspace-data/release/v0.3/windows-package/win-x64`
  - Package includes:
    - `agent\` (self-contained Agent binaries)
    - `desktop-tauri\ceryx-desktop.exe`
    - `config\agent-defaults.env`

### iPad native scaffold and permission verification

- `pnpm --filter @ceryx/ipad build` -> PASS
- `pnpm --filter @ceryx/ipad exec cap init "Ceryx iPad" com.ceryx.ipad --web-dir=dist` -> PASS
- `pnpm --filter @ceryx/ipad exec cap add ios` -> PASS
- `pnpm --filter @ceryx/ipad exec cap sync ios` -> PASS
- Repository-auditable iOS artifacts:
  - `apps/ipad/capacitor.config.ts`
  - `apps/ipad/ios/App/App/Info.plist`
  - `apps/ipad/ios/App/App/en.lproj/InfoPlist.strings`
  - `apps/ipad/ios/App/App/zh-Hans.lproj/InfoPlist.strings`
- Verified Info.plist scope:
  - local network + bonjour service (`_ceryx-agent._tcp`)
  - microphone / photo library read+add
  - file sharing/open-in-place
  - camera / speech recognition extension keys
  - notifications usage description
  - WebView-limited ATS cleartext access

### Viewport Phase 1 real-frame preview update (2026-05-27)

- Implemented `GET /api/v1/capture/frame` authenticated preview route.
- Implemented real Codex-window capture via GDI/PrintWindow fast path.
- Implemented latest-frame in-memory preview service:
  - preview frames are JPEG
  - preview frames do not create files in `Screenshots/`
- Implemented explicit screenshot path on top of the same real capture core:
  - `POST /api/v1/media/screenshot` now writes a real PNG artifact
- Implemented dual-client viewport rendering:
  - `apps/ipad/src/routes/ConsoleRoute.tsx`
  - `apps/desktop/src/routes/ConsoleRoute.tsx`
  - both clients now render real preview frames instead of status-only black panels
- Implemented client-side preview refresh setting:
  - `previewRefreshProfile = balanced | high_frequency`
  - `balanced` -> 1000 ms poll / Agent `mode=balanced`
  - `high_frequency` -> 400 ms poll / Agent `mode=low_latency`
- Implemented degraded behavior:
  - keep last successful frame visible
  - overlay error/state text when refresh later fails

Current verification in this workspace:

- `corepack pnpm typecheck` -> PASS
- `corepack pnpm --filter @ceryx/client-sdk test -- --run` -> PASS
- `corepack pnpm --filter @ceryx/feature-remote-control test -- --run` -> PASS
- `corepack pnpm --filter @ceryx/feature-remote-control build` -> PASS
- `GET /api/v1/capture/frame` transport/test coverage -> added in `agent/windows/tests/Ceryx.Agent.Tests/RemoteControlTransportTests.cs`
- `POST /api/v1/media/screenshot` real-PNG transport/test coverage -> added in `agent/windows/tests/Ceryx.Agent.Tests/RemoteControlTransportTests.cs`
- `getCaptureFrame()` SDK parsing coverage -> added in `packages/client-sdk/src/AgentClient.test.ts`
- `useViewportPreview()` last-frame retention coverage -> added in `packages/feature-remote-control/src/useViewportPreview.test.tsx`
- Non-sandbox local dev/runtime verification:
  - `http://localhost:5173` -> PASS
  - `http://localhost:5174` -> PASS
  - `http://127.0.0.1:41527/api/v1/health` -> PASS
  - `http://192.168.43.156:5174` discovery page -> PASS (`online / unpaired`)

### Viewport Phase 2 TG1+TG2 update (2026-05-31)

- TG1 capture backend abstraction landed:
  - Agent capture now exposes backend selection via `CERYX_CAPTURE_BACKEND=auto|wgc|gdi`.
  - Runtime status adds `captureBackend` field in `GET /api/v1/agent/status`.
  - `auto` backend path implemented with WGC availability gate + GDI fallback path.
- TG2 WebRTC transport chain landed at contract + client state-machine level:
  - `POST /api/v1/capture/webrtc/signal` request/response contract expanded for `offer/answer/ice-candidate/negotiation-needed`.
  - In-memory signal session store added with 60s inactivity cleanup.
  - iPad/Desktop console routes now include WebRTC viewport pipeline with automatic polling fallback.
  - `viewportTransport=webrtc|polling` client setting added on both clients.
- Command evidence from local non-sandbox rerun (2026-06-01):
  - `dotnet workload restore .\agent\windows\Ceryx.Agent.Windows.sln` -> PASS
  - `dotnet restore .\agent\windows\Ceryx.Agent.Windows.sln` -> PASS
  - `dotnet build .\agent\windows\tests\Ceryx.Agent.Tests\Ceryx.Agent.Tests.csproj -c Release --nologo` -> PASS
  - `dotnet test .\agent\windows\tests\Ceryx.Agent.Tests\Ceryx.Agent.Tests.csproj -c Release --nologo --filter "RemoteControlTransportTests.Clipboard"` -> PASS (3/3)
  - `dotnet test .\agent\windows\Ceryx.Agent.Windows.sln -c Release --nologo` -> PASS (96/96)
  - `corepack pnpm typecheck` -> PASS
  - `corepack pnpm --filter @ceryx/protocol test -- --run src/protocol.test.ts` -> PASS (5/5)
  - `corepack pnpm --filter @ceryx/client-sdk test -- --run src/AgentClient.test.ts` -> PASS (29/29)
  - `corepack pnpm --filter @ceryx/feature-remote-control test -- --run src/components.test.tsx` -> PASS (4/4)
  - `corepack pnpm build` -> PASS

### Local runtime smoke verification

- `powershell -ExecutionPolicy Bypass -File .\scripts\smoke-agent-api.ps1 -Token <seeded-local-token>` -> PASS
- `powershell -ExecutionPolicy Bypass -File .\scripts\smoke-local-desktop.ps1 -DesktopMode prod -Token <seeded-local-token>` -> PASS
- `POST /api/v1/agent/open-logs-folder` (Bearer token) -> PASS (`ok=true`, `action=open-logs-folder`)

### First-launch storage verification

- Fresh runtime root verified after Agent first launch:
  - `Logs=True`
  - `Uploads=True`
  - `Screenshots=True`
  - `Recordings=True`
  - `ceryx.db=True`

## Acceptance Mapping (Detailed)

### Backend

- Agent health/status APIs validated by smoke + test suite.
- Storage bootstrap and database initialization validated in fresh runtime root.
- Security hardening and permission coverage validated via W7 test set.
- High-risk confirmation + audit log behavior validated via `HighRiskActions` and settings tests.

### Frontend

Desktop acceptance mapping:

- Local Agent start/status + token path:
  - `apps/desktop/src/App.test.tsx`
  - `scripts/smoke-local-desktop.ps1`
- Local management actions (Open Logs Folder / Restart Agent):
  - `apps/desktop/src/App.test.tsx`
  - `POST /api/v1/agent/open-logs-folder` runtime verification
- Diff / logs / files / tasks / notifications / status bar:
  - `apps/desktop/src/console.files-tasks-notifications.test.tsx`
  - `apps/desktop/src/console.shortcuts-context-menu.test.tsx`
- Settings + high-risk guard:
  - `apps/desktop/src/console.settings.test.tsx`
- Keyboard shortcuts + context menu:
  - `apps/desktop/src/console.shortcuts-context-menu.test.tsx`
- Real viewport preview rendering + settings:
  - `apps/desktop/src/routes/ConsoleRoute.tsx`
  - `packages/feature-remote-control/src/useViewportPreview.ts`

iPad acceptance mapping:

- Discovery / connect / pairing flow entry:
  - `apps/ipad/src/App.test.tsx`
- Native permission baseline and local notification request entry:
  - `apps/ipad/src/routes/LaunchRoute.tsx`
  - `apps/ipad/src/platform/ipad/localNotificationsPermission.ts`
- Live console interaction:
  - `apps/ipad/src/App.test.tsx`
  - `apps/ipad/src/console.gestures-prompt-image.test.tsx`
- Diff + logs + prompt + image upload + voice sheet:
  - `apps/ipad/src/console.gestures-prompt-image.test.tsx`
- Files / tasks / notifications:
  - `apps/ipad/src/console.files-tasks-notifications.test.tsx`
- Settings persistence:
  - `apps/ipad/src/console.settings.test.tsx`
- Real viewport preview rendering + settings:
  - `apps/ipad/src/routes/ConsoleRoute.tsx`
  - `packages/feature-remote-control/src/useViewportPreview.ts`

### Security

- Route-permission matrix present.
- Local-only routes enforced.
- High-risk action gates and audit events covered by tests.

## Current Blockers

1. No repository-structure blocker remains for Phase 1 viewport preview delivery.
2. No local Windows verification blocker remains for the TG1+TG2 contract/state-machine path; full dotnet + pnpm validation reran successfully on 2026-06-01.

## External Validation Pending

1. Xcode Debug runtime verification was not executed in this Windows environment and must be completed on macOS host.

## Conclusion

W8 acceptance evidence now includes a real-frame Phase 1 viewport implementation for iPad and desktop, with authenticated in-memory JPEG preview frames and real PNG screenshots. TG1+TG2 contract/state-machine validation (dotnet + pnpm) has been rerun successfully in local non-sandbox PowerShell. Remaining external validation is macOS/Xcode runtime verification for the iPad native shell.
